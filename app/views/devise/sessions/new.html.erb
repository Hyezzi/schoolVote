
<div class="content d-flex justify-content-center text-center">


<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  
  <br>
  <br>
  <br>
  <h4>관리자만 접근할 수 있습니다!</h4>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div class="field">
    <%= f.label :password %><br />
    <%= f.password_field :password, autocomplete: "off" %>
  </div>

  <%# <% if devise_mapping.rememberable? -%> 
    <%# <div class="field"> %>
      <%# <%= f.check_box :remember_me %> 
      <%# <%= f.label :remember_me %> 
    <%# </div> %>
  <%# <% end -%> 

<div class="actions">
    <%#= f.submit t('.sign_in') %>
     <button type="submit">Login</button>
  </div>



<%# <%= render "devise/shared/links" %> 
<% end %>

<style>
button {
    background-color: rgb(139, 44, 49);
    color: white;
    padding: 14px 20px;
    margin: 8px 0;
    border: none;
    cursor: pointer;

}
.field {
    width: 100%;
    padding: 12px 20px;
    margin: 8px 0;
}

.container_login {
    font-size: 30px;
    text-align: center;
}

.linktext {

    color: black;

}
</style> 
